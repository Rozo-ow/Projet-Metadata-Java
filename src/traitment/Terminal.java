import org.apache.commons.cli.*;

import java.nio.file.Files;
import java.nio.file.Paths;

/**
public class Terminal {
    switch (args[0]) {
        case "h":
            // affiche l'aide

            break;
        case "d":
            // affiche les métadonnées d'un fichier
            break;
        case "f":
            // affiche les métadonnées d'un fichier
            break;
        default:
            throw new IllegalArgumentException("Commande non reconnue : " + args[0]);
    }
}
*/